<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
        integrity="sha384-r4kXmG5F5u5sk1VxE6f6HJ9jlF5Vo2FNn5c5v8w5t5tiD5MvqIHfKfj5A5f5p5tkBn"
        crossorigin="anonymous">
<!-- 제품 상세 정보 카드 -->
<div class="product-card">
    <button id="back-button" class="back-button"><i class="fas fa-home"></i> Back </button> <!-- HOME 버튼에 id 추가 -->
    {{#carrier}}
    <div class="brand">{{brand}}</div>
    <div class="product-image">
        <img src="{{image}}" alt="Product Image" class="centered-image">
    </div>
    <div class="product-details">
        <div class="product-text">
            <div class="product-info">
                <p class="price">가격 : {{price}}</p>
                <p class="size">크기 : {{size}}</p>
            </div>
            <div class="product-info">
                <p class="capacity">용량 : {{capacity}}</p>
                <p class="doorCount">문개수 : {{doorCount}}</p>
            </div>
            <div class="product-info">
                <p class="powerConsumptionGrade">전력소비량 : {{powerConsumptionGrade}}</p>
                <p class="numberOfUsers">추천가구 : {{numberOfUsers}}</p>
            </div>
        </div>
    </div>
    {{/carrier}}
</div>

<script>
    // HOME 버튼 클릭 시 뒤로가기
    document.getElementById('back-button').addEventListener('click', function() {
        window.history.back(); // 뒤로가기 기능 실행
    });
</script>


<style>
    /* 페이지 전체 배경을 그대로 유지 */
    body {
        background-color: gray;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
    }

    /* 배경에 화려한 빛나는 효과 애니메이션 추가 */
    @keyframes shine {
        0% {
            background-color: gray;
        }
        50% {
            background-color: silver;
        }
        100% {
            background-color: gray;
        }
    }

    /* 제품 카드 스타일 설정 */
    .product-card {
        background-color: white;
        padding: 20px;
        border-radius: 20px; /* 더 둥근 모서리로 조정 */
        box-shadow: 0 40px 80px rgba(0, 0, 0, 0.6); /* 더 강한 그림자 스타일 */
        width: 600px; /* 가로 너비를 더 크게 조절 */
        height: 800px; /* 높이를 더 크게 조절 */
        display: flex;
        flex-direction: column;
        align-items: center; /* 가운데 정렬 */
        transform: rotateX(20deg); /* X 축을 기준으로 강한 회전 */
    }

    /* HOME 버튼 스타일 수정 */
    .product-card button {
        background-color: transparent; /* 배경 투명하게 설정 */
        border: none; /* 테두리 제거 */
        outline: none; /* 포커스 효과 제거 */
        cursor: pointer;
        position: absolute;
        top: 20px;
        left: 20px; /* 원하는 위치로 조정 */
        font-size: 12px;
        color: ;
    }

    /* 반짝이는 효과 애니메이션 추가 */
    @keyframes blink {
        0% {
            opacity: 1;
        }
        50% {
            opacity: 0.5;
        }
        100% {
            opacity: 1;
        }
    }

    /* 반짝이는 효과를 HOME 버튼에 적용 */
    .product-card button:hover {
        animation: blink 1s infinite; /* 반짝이는 애니메이션 적용 */
    }

    .product-card .product-image {
        display: flex;
        flex-direction: column; /* 이미지와 텍스트를 세로로 배치 */
        align-items: center; /* 가운데 정렬 */
        max-height: 100%;
        width: auto;
        margin-top: 70px; /* 이미지와 텍스트 사이의 간격 조절 */
    }

    /* 이미지 크기 일정하게 조절 */
    .centered-image {
        max-width: 300%; /* 최대 너비 100%로 설정 */
        max-height: 500px; /* 원하는 높이로 설정 */
        display: block;
        margin: 0 auto;
    }

    .brand {
        text-align: center;
        font-weight: bold;
        font-size: 30px;
        color: #808080;
        margin-top: 25px;
        margin-bottom: -30px;
    }

    .product-details {
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: -25px; /* 좀 더 위로 올림 */
        color: #808080;
        font-weight: bold;
    }
    .product-info {
        display: flex;
        justify-content: space-between;
        width: 100%;
        margin-right: 200px; /* 각 요소 사이의 간격 조절 */
    }

    .product-text {
        text-align: center;
        margin-top: 30px; /* 텍스트 사이의 간격 조절 */
    }

    .price, .size, .capacity, .doorCount, .powerConsumptionGrade, .numberOfUsers {
        font-size: 18px;
        width: calc(50% - 5px); /* 2개의 요소를 가로로 나누고 간격 조절 */
        margin: 0; /* 기본 마진 제거 */
        padding: 15px; /* 요소 주위의 간격 조절 */
        text-align: center;
        white-space: nowrap; /* 텍스트가 한 줄로 줄 바꿈되지 않도록 설정 */
        overflow: hidden; /* 텍스트가 요소 너비를 넘어갈 경우 숨김 처리 */
        text-overflow: ellipsis; /* 텍스트가 요소 너비를 넘어갈 경우 말줄임표 (...)로 표시 */
        display: inline-block; /* 요소를 인라인 블록 요소로 표시하여 비뚤빼뚤한 배치 방지 */
    }

    /* 배경에 화려한 빛나는 효과 애니메이션 적용 */
    body {
        animation: shine 4s linear infinite;
    }
    .size {
    font-size: 18px;
    width: 100%; /* 요소의 가로 너비를 100%로 설정하여 한 줄로 표시 */
    margin: 0; /* 기본 마진 제거 */
    padding: 15px; /* 요소 주위의 간격 조절 */
    text-align: center;
    white-space: nowrap; /* 텍스트가 한 줄로 줄 바꿈되지 않도록 설정 */
    overflow: hidden; /* 텍스트가 요소 너비를 넘어갈 경우 숨김 처리 */
    text-overflow: ellipsis; /* 텍스트가 요소 너비를 넘어갈 경우 말줄임표 (...)로 표시 */
    }
</style>

